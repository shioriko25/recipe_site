


<% customer_recipe.each do |recipe| %>

  <%= image_tag recipe.get_image, size: '50x50' %>
  <%= link_to recipe.customer.name, customer_path(recipe.customer.id) %>
  <%= link_to recipe.name, recipe_path(recipe.id) %>
  コメント数：<%= recipe.comments.count %>

<% end %>

